export function PhotoPreview({ src, fileName, onGenerate, onReplace }: { src: string; fileName: string; onGenerate: () => void; onReplace: () => void }) {
  return <main className="mobile-flow"><div className="eyebrow">STEP_02: PREVIEW_FRAME</div><h1>LOOKING GOOD.</h1><p>Ready to apply the HH Goa 2026 frame to your upload.</p><div className="preview-photo"><img src={src} alt="Selected upload"/><div className="preview-overlay"><b>HH GOA 2026</b><span>BUILDER</span></div></div><div className="file-meta"><span>{fileName}</span><span>1080 × 1080 px</span></div><section className="preview-note"><span className="note-sticker">STEP 02</span><div><b>Preview Frame Overlay</b><p>The final generation applies high-res framing, typography and colour treatment.</p></div><button className="outline-button" onClick={onReplace}>REPLACE PHOTO</button></section><button className="generate-button" onClick={onGenerate}>GENERATE FRAME <span>↗</span></button></main>
}
